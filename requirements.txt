# Core FastAPI
fastapi==0.99.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# HTTP requests
aiohttp==3.9.1
requests==2.31.0

# Supabase (database + pgvector)
supabase>=2.0.0
psycopg2-binary>=2.9.9  # PostgreSQL adapter
asyncpg>=0.29.0  # Async PostgreSQL

# Migration tools
tqdm>=4.66.0  # Progress bars para scripts de migração

# Web scraping / browser automation
beautifulsoup4==4.12.2
lxml==4.9.3
selenium==4.14.0
webdriver-manager==4.0.2

# Image processing
Pillow==10.1.0

# Embeddings / semantic search / LLM
openai>=1.0.0
numpy<2.0.0
sentence-transformers>=2.2.2
tiktoken>=0.4.0
faiss-cpu>=1.7.4  # (Mantido apenas se ainda for usado em testes locais; Supabase pgvector é a fonte principal)
tenacity>=8.2.3  # Retries robustos (usado em rag_pipeline)

# Optional helpers
python-dateutil==2.8.2

# Data augmentation
googletrans==4.0.1
pandas>=1.5.0
schedule>=1.2.0

# Sistema Dual-Stack + Diferenciais
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0
# Áudio / Voz (opcional - comentar se não usar voz)
pydub>=0.25.1
SpeechRecognition>=3.10.0

Jinja2>=3.1.2

# Firebase removido: firebase-admin / google-cloud-firestore não são mais necessários